# description of this workflow, can be anything you want
name: RequestWagoData

# we need to let GitHub know _when_ we want to release, typically only when we create a new tag.
# this will target only tags, and not all pushes to the master branch.
# this part can be heavily customized to your liking, like targeting only tags that match a certain word,
# other branches or even pullrequests.
on:
  workflow_dispatch: 
   

# a workflow is built up as jobs, and within these jobs are steps
jobs:

  # "request" is a job, you can name it anything you want
  Request:

    # we can run our steps on pretty much anything, but the "ubuntu-latest" image is a safe bet
    runs-on: ubuntu-latest

    # specify the environment variables used by the packager, matching the secrets from the project on GitHub
    env:
      GITHUB_OAUTH: ${{ secrets.GITHUB_TOKEN }}  # "GITHUB_TOKEN" is a secret always provided to the workflow
                                                 # for your own token, the name cannot start with "GITHUB_"

    # "steps" holds a list of all the steps needed
    steps:
      - name: Clone project
        uses: actions/checkout@v3
        
      - name: fetchData
        uses: JamesIves/fetch-api-data-action@v2
        id: fetch-api-data
        with:
          endpoint: https://data.wago.io/lookup/wago?id=RaidAbilityTimeline
      - name: print result
        run: echo ${{ steps.fetchData.outputs.fetch-api-data}}
        
      - name: Overwrite file
        uses: "DamianReeves/write-file-action@master"
        with:
          path: CrossOriginStuff/WeakAuras/RaidAbilityTimeline.json
          write-mode: overwrite
          contents: |
            ${{ steps.fetchData.outputs.fetch-api-data}}
        
      - name: Build and Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
           branch: main # Pushes the updates to the main branch.
           folder: CrossOriginStuff/WeakAuras/RaidAbilityTimeline.json # The location of the data.json file saved by the Fetch API Data action.
           target-folder: CrossOriginStuff/WeakAuras/ # Saves the data into the directory on the main branch.     
